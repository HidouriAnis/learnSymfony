{% extends 'layout.html.twig' %}

{% block title %}
    {{ parent() }} Register
{% endblock %}

{% block content %}
    {{ form_start(form) }}
        {{ form_rest(form) }}
        <button type="submit">{{ 'button.submit'|trans }}</button>
    {{ form_end(form) }}
{% endblock %}
